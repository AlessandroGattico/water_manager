@page
@using WaterManagerUI.Model.Item
@model WaterManagerUI.Pages.Azienda.Campagne

<link rel="stylesheet" href="~/css/site.css"/>


<div class="container-fluid">
    <div class="row">
        <div class="col-md-2">
            @Html.Partial("layout/Admin/_SideMenuAdmin")
        </div>

        <div class="col-md-8">
            <div class="main-container">
                <div class="content">
                    <h2>Visualizza campagne</h2>
                </div>
            </div>
            @if (Model.campagne.Any())
            {
                <table class="table">
                    <thead>
                    <tr>
                        <th>Nome</th>
                    </tr>
                    </thead>
                    <tbody>

                    @foreach (Campagna campagna in Model.campagne)
                    {
                        <tr>
                            <td>@campagna.nome</td>
                        </tr>
                    }
                    </tbody>
                </table>
            }
            else
            {
                <p>Non sono presenti campagne</p>
            }
        </div>

        <div class="col-md-2">
        </div>
    </div>
</div>