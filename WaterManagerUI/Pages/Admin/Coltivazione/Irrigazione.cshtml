@page
@model WaterManagerUI.Pages.Admin.Irrigazione

<link rel="stylesheet" href="~/css/Visualizza.css"/>
<link rel="stylesheet" href="~/css/site.css"/>

<div class="container-fluid">
    <div class="row">
        <div class="col-md-2">
            @Html.Partial("layout/Admin/_SideMenuAdmin")
        </div>

        <div class="col-md-8">
            <div class="main-content">
                <div class="content">
                    <h2>Visualizza tipologie irrigazione</h2>
                </div>
            </div>

            @if (Model.irrigazioni.Any())
            {
                <div class="bottoni-azione">
                    <div class="bottone-aggiungi">
                        <a asp-page="/Admin/Crea/CreaIrrigazione" class="plus-icon">+</a>
                        <a asp-page="/Admin/Crea/CreaIrrigazione">Aggiungi irrigazione</a>
                    </div>
                    <div class="bottone-aggiungi" style=" border: 2px dashed #ff0000; color: #ff0000">
                        <a asp-page="/Admin/Elimina/EliminaIrrigazione" class="plus-icon">-</a>
                        <a asp-page="/Admin/Elimina/EliminaIrrigazione">Elimina irrigazione</a>
                    </div>
                </div>
            }
            else
            {
                <div class="bottone-aggiungi">
                    <a asp-page="/Admin/Crea/CreaIrrigazione" class="plus-icon">+</a>
                    <a asp-page="/Admin/Crea/CreaIrrigazione">Aggiungi irrigazione</a>
                </div>
            }
            @if (Model.irrigazioni.Any())
            {
                <table class="table">
                    <thead>
                    <tr>
                        <th>Nome</th>
                    </tr>
                    </thead>
                    <tbody>

                    @foreach (String irrigazione in Model.irrigazioni)
                    {
                        <tr>
                            <td>@irrigazione</td>
                        </tr>
                    }
                    </tbody>
                </table>
            }
        </div>

        <div class="col-md-2">
        </div>
    </div>
</div>