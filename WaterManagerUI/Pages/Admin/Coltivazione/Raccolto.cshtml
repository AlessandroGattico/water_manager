@page
@model WaterManagerUI.Pages.Admin.Raccolto

<link rel="stylesheet" href="~/css/Visualizza.css"/>
<link rel="stylesheet" href="~/css/site.css"/>

<div class="container-fluid">
    <div class="row">
        <div class="col-md-2">
            @Html.Partial("layout/Admin/_SideMenuAdmin")
        </div>

        <div class="col-md-8">
            <div class="main-content">
                <div class="content">
                    <h2>Visualizza tipologie raccolto</h2>
                </div>
            </div>

            @if (Model.raccolti.Any())
            {
                <div class="bottoni-azione">
                    <div class="bottone-aggiungi">
                        <a asp-page="/Admin/Crea/CreaRaccolto" class="plus-icon">+</a>
                        <a asp-page="/Admin/Crea/CreaRaccolto">Aggiungi raccolto</a>
                    </div>
                    <div class="bottone-aggiungi" style=" border: 2px dashed #ff0000; color: #ff0000">
                        <a asp-page="/Admin/Elimina/EliminaRaccolto" class="plus-icon">-</a>
                        <a asp-page="/Admin/Elimina/EliminaRaccolto">Elimina raccolto</a>
                    </div>
                </div>
            }
            else
            {
                <div class="bottone-aggiungi">
                    <a asp-page="/Admin/Crea/CreaRaccolto" class="plus-icon">+</a>
                    <a asp-page="/Admin/Crea/CreaRaccolto">Aggiungi raccolto</a>
                </div>
            }
            
            @if (Model.raccolti.Any())
            {
                <table class="table">
                    <thead>
                    <tr>
                        <th>Nome</th>
                    </tr>
                    </thead>
                    <tbody>

                    @foreach (String raccolto in Model.raccolti)
                    {
                        <tr>
                            <td>@raccolto</td>
                        </tr>
                    }

                    </tbody>
                </table>
            }
        </div>

        <div class="col-md-2">
        </div>
    </div>
</div>