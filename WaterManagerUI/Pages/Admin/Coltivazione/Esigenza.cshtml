@page
@model WaterManagerUI.Pages.Admin.Esigenza

<link rel="stylesheet" href="~/css/Visualizza.css"/>
<link rel="stylesheet" href="~/css/site.css"/>


<div class="container-fluid">
    <div class="row">
        <div class="col-md-2">
            @Html.Partial("layout/Admin/_SideMenuAdmin")
        </div>

        <div class="col-md-8">
            <div class="main-container">
                <div class="content">
                    <h2>Visualizza tipologie esigenza</h2>
                </div>
            </div>

            @if (Model.esigenze.Any())
            {
                <div class="bottoni-azione">
                    <div class="bottone-aggiungi">
                        <a asp-page="/Admin/Crea/CreaEsigenza" class="plus-icon">+</a>
                        <a asp-page="/Admin/Crea/CreaEsigenza">Aggiungi esigenza</a>
                    </div>

                    <div class="bottone-aggiungi" style=" border: 2px dashed #ff0000; color: #ff0000">
                        <a asp-page="/Admin/Elimina/EliminaEsigenza" class="plus-icon">-</a>
                        <a asp-page="/Admin/Elimina/EliminaEsigenza">Elimina esigenza</a>
                    </div>
                </div>
            }
            else
            {
                <div class="bottone-aggiungi">
                    <a asp-page="/Admin/Crea/CreaEsigenza" class="plus-icon">+</a>
                    <a asp-page="/Admin/Crea/CreaEsigenza">Aggiungi esigenza</a>
                </div>
            }
            @if (Model.esigenze.Any())
            {
                <table class="table">
                    <thead>
                    <tr>
                        <th>Nome</th>
                    </tr>
                    </thead>
                    <tbody>
                    @foreach (String esigenza in Model.esigenze)
                    {
                        <tr>
                            <td>@esigenza</td>
                        </tr>
                    }
                    </tbody>
                </table>
            }
        </div>
        <div class="col-md-2">
        </div>
    </div>
</div>