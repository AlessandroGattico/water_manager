@page
@model WaterManagerUI.Pages.Elimina.EliminaRaccolto

<link rel="stylesheet" href="~/css/site.css"/>


<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <h2>Crea azienda</h2>
            <form method="post">
                <div class="form-group">
                    <label asp-for="raccolto">Raccolto</label>
                    <select asp-for="raccolto" class="form-control">
                        @foreach (String element in Model.raccolti)
                        {
                            <option>@element</option>
                        }
                    </select>
                </div>
                <div class="form-group">
                    <button type="submit" class="btn btn-primary form-button">Elimina raccolto</button>
                </div>
            </form>
        </div>
    </div>
</div>