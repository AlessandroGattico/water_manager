@page
@model WaterManagerUI.Pages.Elimina.EliminaIrrigazione

<link rel="stylesheet" href="~/css/site.css"/>


<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <h2>Crea azienda</h2>
            <form method="post">
                <div class="form-group">
                    <label asp-for="irrigazione">Irrigazione</label>
                    <select asp-for="irrigazione" class="form-control">
                        @foreach (String element in Model.irrigazioni)
                        {
                            <option>@element</option>
                        }
                    </select>
                </div>
                <div class="form-group">
                    <button type="submit" class="btn btn-primary form-button">Elimina irrigazione</button>
                </div>
            </form>
        </div>
    </div>
</div>